#include<stdio.h>
int main(){
    int n,count=0;
    printf("Enter a number:");
    scanf("%d",&n);
    if(n==0){
        return 1;
    }
    while(n!=0)
    {
        n=n/10;
        ++count;
    }
    printf("No of digits:%d",count);
    return 0;
}
